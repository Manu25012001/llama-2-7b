torch==2.0.1            # PyTorch for building and training the model
transformers==4.12.0    # Hugging Face library for model utilities and tokenizers
datasets==2.6.1         # For loading and managing datasets
sentencepiece==0.1.96   # Sentence tokenization (required for LLaMA models)
tokenizers==0.12.1      # Tokenization utilities
tqdm==4.62.3            # For progress bars during training
numpy==1.21.2           # Numerical operations
scikit-learn==0.24.2    # For evaluation metrics and utilities
pandas==1.3.3           # For data manipulation and analysis
matplotlib==3.4.3       # For visualization of training progress
accelerate==0.12.0      # Optimized hardware acceleration for distributed training
